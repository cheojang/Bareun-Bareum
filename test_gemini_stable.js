const { GoogleGenerativeAI } = require('@google/generative-ai'); const dotenv = require('dotenv'); dotenv.config({path: '.env.local'}); const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY); async function test(m) { try { const r = await genAI.getGenerativeModel({model: m}).generateContent('hi'); console.log(m, 'SUCCESS'); } catch (e) { console.log(m, 'FAILED', e.message); } } test('gemini-flash-latest'); test('gemini-2.5-flash'); test('gemini-1.5-flash'); test('gemini-2.0-flash');
